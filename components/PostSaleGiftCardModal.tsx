// This component is obsolete and has been removed.
// The gift card workflow has been streamlined. After a gift card sale,
// the system now intelligently decides whether it's a top-up or a new physical card
// and directly presents a new, unified "ShareableGiftCardModal".
// This eliminates the confusing intermediate choice for the user.
import React from 'react';

const PostSaleGiftCardModal = () => null;

export default PostSaleGiftCardModal;
